import { Meta } from '@storybook/addon-docs/blocks';

<Meta title="Introduction" />

export const statusTag = (bg, fg, border) => ({
  display: 'inline-block',
  padding: '1px 6px',
  borderRadius: '4px',
  fontSize: '10px',
  fontWeight: 500,
  letterSpacing: 0,
  lineHeight: '16px',
  color: fg,
  backgroundColor: bg,
  border: `1px solid ${border}`,
});

export const stableTag = statusTag('rgba(46, 125, 50, 0.14)', '#2e7d32', 'rgba(46, 125, 50, 0.3)');
export const experimentalTag = statusTag(
  'rgba(106, 63, 209, 0.14)',
  '#6a3fd1',
  'rgba(106, 63, 209, 0.3)'
);
export const needsReviewTag = statusTag(
  'rgba(180, 83, 9, 0.14)',
  '#b45309',
  'rgba(180, 83, 9, 0.3)'
);
export const deprecatedTag = statusTag(
  'rgba(198, 40, 40, 0.14)',
  '#c62828',
  'rgba(198, 40, 40, 0.3)'
);

export const tagCell = { display: 'inline-block', width: '110px' };

# Welcome to Peak Design

Peak Design is Percona's design system and UI library containing the guidelines and assets that allow Perconians and the community to create functional, professional, and attractive GUIs for our software products.

While our Figma kit represents our design intent, **this Storybook is our definitive coded source of truth**. What you see here is exactly what renders in the UI codebase and Percona products.

## Maturity tags

Every documented component carries a **maturity tag** so humans and agents can tell at a glance what's safe to build on and what needs care. The tag renders as a colored badge next to the component in the sidebar and on its docs page. You can also filter the sidebar by status using the filter menu (the funnel icon at the bottom of the sidebar), and each tag is present in the generated `index.json` for tooling.

Exactly one status applies per component, from this closed set:

<div style={{ display: 'flex', flexDirection: 'column', gap: '0.75rem', margin: '1.25rem 0' }}>
  <div>
    <span style={tagCell}>
      <span style={stableTag}>Stable</span>
    </span>
    Production-ready and reviewed. Safe to use in any product — copy the snippets with confidence.
  </div>
  <div>
    <span style={tagCell}>
      <span style={experimentalTag}>Experimental</span>
    </span>
    Intentionally early. The API may change without notice; pin to a version and expect churn.
  </div>
  <div>
    <span style={tagCell}>
      <span style={needsReviewTag}>Needs review</span>
    </span>
    Functional but not yet vetted against the design system. Usable, but may change after review —
    check with us before relying on it.
  </div>
  <div>
    <span style={tagCell}>
      <span style={deprecatedTag}>Deprecated</span>
    </span>
    On the way out. Avoid in new code and migrate existing usage to the recommended replacement.
  </div>
</div>

Components grouped under **To be reviewed** in the sidebar are all tagged `needs review`. The absence of a tag means we haven't classified it yet — treat it as unreviewed.

By default the sidebar hides `needs review` and `deprecated` entries so you see the safe set first. Storybook marks the filter as active (the funnel icon at the bottom of the sidebar is highlighted) — open it and clear the status filters to reveal everything.

## Resources

- [Source code](https://github.com/percona/percona-ui): The source code for the Peak Design library and Storybook.
- [MUI docs](https://mui.com/material-ui/): The core library that Peak Design is built on top of.
- [Material React Table docs](https://www.material-react-table.com/): The library powering our [Table component](?path=/docs/data-display-table--docs).
- [Figma kit](https://www.figma.com/design/08jGF3GZAUGmazlQtk0UQk/Peak-Design-Kit): If you're interested in making mockups for new software ideas or iterate on Percona products.

## Recommended workflow

For the best experience, treat this Storybook as your first stop:

1. **Search Storybook first**: Identify if the component or token you need is already built and documented here.
2. **Verify the design**: Use the "Design" tab, if available, to ensure the coded component matches the Figma spec.
3. **Implement**: Copy the code snippets and use them in your feature branch.
4. **Missing a component?** If a component exists in the Peak Design Figma kit or MUI but isn't here yet, it's time to build it and add it to the Storybook! Contact us to learn about the contribution guidelines.

## Contacts

Create an [issue](https://github.com/percona/percona-ui/issues) for bugs, request features, or ask questions. Or, if you're a Perconian, reach the PMM team in the #pmm Slack channel for quicker feedback.
